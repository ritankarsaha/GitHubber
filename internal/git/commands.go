package git

import (
	// "os/exec"
)

// RunCommand executes a shell command and returns the output or error
// func RunCommand(command string) (string, error) {
// 	cmd := exec.Command("sh", "-c", command) 
// 	output, err := cmd.CombinedOutput()
// 	return string(output), err
// }

